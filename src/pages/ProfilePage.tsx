// ProfilePage.tsx — DEPRECATED: Ce composant n'est plus utilisé.
// Redirige vers SellerProfilePage (vendeur) ou BuyerProfilePage (acheteur)
// via App.tsx routing. Fichier conservé pour éviter les erreurs d'import résiduel.
export function ProfilePage() { return null; }
